import React from "react";
import SocialTabs from "./SocialTabs";

// Expo-router / file-based routing: visiting the /social route will render the SocialTabs navigator
export default function SocialRoute() {
  return <SocialTabs />;
}
